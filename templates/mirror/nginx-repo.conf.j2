server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;
    root {{ mirror_root }};
    autoindex on;
    charset utf-8;

    location / {
        try_files $uri =404;
    }
}